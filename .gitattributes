# 統一換行處理。
#
# 這個專案只在 Windows 上跑，但 GitHub 下載下來的檔案要確保：
#   1. PowerShell 腳本是 CRLF（PowerShell 5.1 對 LF 容忍度差）
#   2. 字型與圖片不會被當成文字檔改壞
#   3. .ps1 的 UTF-8 BOM 不會遺失（沒有 BOM，中文註解會讓 PS 5.1 解析失敗）

* text=auto

# PowerShell：一定要 CRLF
*.ps1 text eol=crlf

# Windows 設定檔
*.config text eol=crlf
*.bat    text eol=crlf
*.cmd    text eol=crlf

# 由 cmd 的 type 指令印出的說明檔，要 CRLF 才不會擠成一行
*.txt    text eol=crlf

# 原始碼：維持 LF，PHP 與瀏覽器都不在意
*.php  text eol=lf
*.stub text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.md   text eol=lf
*.json text eol=lf

# 二進位檔：絕對不要做換行轉換
*.woff  binary
*.woff2 binary
*.ttf   binary
*.eot   binary
*.png   binary
*.jpg   binary
*.gif   binary
*.ico   binary
*.zip   binary

# GitHub 語言統計不要把第三方套件算進來
public/assets/vendor/** linguist-vendored
vendor/**               linguist-vendored
